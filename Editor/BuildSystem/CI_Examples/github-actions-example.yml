# GitHub Actions Example for Molca Build System
# Place this file in .github/workflows/build.yml in your repository root

name: Unity Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      profile:
        description: 'Build Profile'
        required: true
        default: 'development'
        type: choice
        options:
          - development
          - staging
          - production

env:
  UNITY_VERSION: 6000.0.x  # Change to your Unity version

jobs:
  build-windows:
    name: Build for Windows
    runs-on: windows-latest
    
    steps:
      # Checkout repository
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          lfs: true
      
      # Cache Unity Library folder
      - name: Cache Unity Library
        uses: actions/cache@v3
        with:
          path: Library
          key: Library-Windows-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-Windows-
      
      # Build
      # Build correctness is checked here rather than inside the build, because game-ci passes
      # '-quit' (see customParameters below). RunCI exits non-zero on any Error finding, so a
      # project with an invalid build profile, version, scene list or content package never
      # reaches the build step.
      - name: Molca Doctor
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          buildMethod: Molca.Editor.Doctor.MolcaDoctor.RunCI
          allowDirtyBuild: true

      - name: Build project
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: StandaloneWindows64
          buildMethod: Molca.Editor.CommandLineBuild.BuildProduction
          versioning: None
          # game-ci's builder bakes '-quit' into its command line, which would terminate Unity
          # before Molca's asynchronous pre-build gate could run. The opt-out below acknowledges
          # that; the Doctor step above runs the same checks as a separate job instead.
          customParameters: -molcaSkipBuildGate
      
      # Upload build artifacts
      - name: Upload build
        uses: actions/upload-artifact@v3
        with:
          name: Build-Windows
          path: Builds/
          retention-days: 7
      
      # Optional: Upload to itch.io
      # - name: Deploy to itch.io
      #   uses: manleydev/butler-publish-itchio-action@v1.0.3
      #   env:
      #     BUTLER_CREDENTIALS: ${{ secrets.BUTLER_CREDENTIALS }}
      #     CHANNEL: windows
      #     ITCH_GAME: your-game
      #     ITCH_USER: your-username
      #     PACKAGE: Builds/Windows
  
  build-android:
    name: Build for Android
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          lfs: true
      
      - name: Cache Unity Library
        uses: actions/cache@v3
        with:
          path: Library
          key: Library-Android-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-Android-
      
      # Build correctness is checked here rather than inside the build, because game-ci passes
      # '-quit' (see customParameters below). RunCI exits non-zero on any Error finding, so a
      # project with an invalid build profile, version, scene list or content package never
      # reaches the build step.
      - name: Molca Doctor
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          buildMethod: Molca.Editor.Doctor.MolcaDoctor.RunCI
          allowDirtyBuild: true

      - name: Build project
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: Android
          buildMethod: Molca.Editor.CommandLineBuild.BuildProduction
          versioning: None
          # game-ci's builder bakes '-quit' into its command line, which would terminate Unity
          # before Molca's asynchronous pre-build gate could run. The opt-out below acknowledges
          # that; the Doctor step above runs the same checks as a separate job instead.
          customParameters: -molcaSkipBuildGate
      
      - name: Upload build
        uses: actions/upload-artifact@v3
        with:
          name: Build-Android
          path: Builds/Android/*.apk
          retention-days: 7

# Setup Instructions:
# 1. Copy this file to .github/workflows/build.yml
# 2. Set up Unity license secrets in repository settings:
#    - UNITY_LICENSE (activation file)
#    - UNITY_EMAIL
#    - UNITY_PASSWORD
# 3. Push to trigger the build
# 4. Download artifacts from Actions tab

