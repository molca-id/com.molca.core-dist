namespace Molca.Events
{
    /// <summary>
    /// Network communication events
    /// </summary>
    public static partial class TypedEvents
    {
        // Network events (placeholders - implement when network system is added)
        // public static readonly Event Connected = new Event(EventConstants.Network.Connected);
        // public static readonly Event<string> Disconnected = new Event<string>(EventConstants.Network.Disconnected);
        // public static readonly Event DataReceived = new Event(EventConstants.Network.DataReceived);
    }
}
